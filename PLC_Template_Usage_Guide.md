# PLC程序开发模板使用指南

## 模板系统概述

本模板系统为三菱FX5U PLC程序开发提供了标准化的框架，包含完整的ST程序结构和对应的XML导入文件。

## 模板文件说明

### 核心模板文件

1. **`PLC_Program_Template.st`** - 完整的ST程序模板
   - 包含标准化的变量定义结构
   - 完整的程序逻辑框架
   - 详细的注释和使用说明

2. **`PLC_Template_XML_Generator.xml`** - 对应的XML导入文件
   - 符合GX Works3 IEC 61131-10格式
   - 包含基本I/O配置
   - 可直接导入GX Works3

## 模板特点和优势

### 1. 标准化结构
- **分层变量定义**: 输入、输出、内部变量分类清晰
- **统一命名规范**: 全局变量使用g前缀，便于识别
- **模块化设计**: 不同功能模块独立，便于维护

### 2. 完整的功能覆盖
- **基本控制**: 启动/停止/复位逻辑
- **保护功能**: 急停、过载、热保护
- **故障处理**: 多级故障检测和自动复位
- **状态管理**: 完整的状态机设计
- **定时控制**: 标准定时器配置
- **计数功能**: 启动次数和故障次数统计

### 3. 扩展性设计
- **模拟量支持**: 预留模拟量输入输出
- **通信接口**: 预留通信处理框架
- **数据记录**: 预留历史数据处理

## 快速使用步骤

### 步骤1: 复制模板
```bash
# 复制ST模板
copy PLC_Program_Template.st YourProject_Program.st

# 复制XML模板  
copy PLC_Template_XML_Generator.xml YourProject.xml
```

### 步骤2: 修改项目信息
在ST文件头部修改:
```st
项目名称: [填写具体项目名称]
功能描述: [描述项目功能]
作者: [作者姓名]
创建日期: [YYYY-MM-DD]
```

### 步骤3: 配置I/O映射
根据实际硬件修改I/O分配:
```st
// 修改输入定义
gStart_Button    : BOOL := FALSE;    // X0 -> 实际地址
gStop_Button     : BOOL := FALSE;    // X1 -> 实际地址

// 修改输出定义  
gMain_Contactor  : BOOL := FALSE;    // Y0 -> 实际地址
```

### 步骤4: 实现具体逻辑
在预留的程序段中添加具体控制逻辑:
```st
(* ========== 主控制逻辑 ========== *)
// 在此添加项目特定的控制算法
```

### 步骤5: 更新XML文件
- 修改XML中的项目名称和变量地址
- 确保与ST文件中的I/O映射一致

## 不同应用场景的模板定制

### 1. 简单电机控制
```st
// 保留基本的启停控制
// 删除不需要的模拟量和通信部分
// 简化状态机逻辑
```

### 2. 复杂设备控制
```st
// 扩展状态机状态数量
// 增加设备特定的保护逻辑
// 添加设备状态反馈处理
```

### 3. 过程控制应用
```st
// 重点使用模拟量处理部分
// 添加PID控制算法
// 扩展数据记录功能
```

### 4. 多设备协调控制
```st
// 扩展通信处理部分
// 添加设备间协调逻辑
// 增强故障处理机制
```

## 最佳实践建议

### 1. 变量命名规范
- **全局变量**: g前缀 (如: gStart_Button)
- **局部变量**: 无前缀 (如: Start_Condition)
- **常数**: 全大写 (如: START_DELAY_TIME)
- **状态变量**: State后缀 (如: System_State)

### 2. 程序结构规范
- 按功能模块组织代码
- 每个模块前添加清晰的注释分隔
- 保持代码缩进一致
- 使用有意义的变量和函数名

### 3. 错误处理规范
- 所有保护输入都要检查
- 故障要有明确的故障码
- 提供自动和手动复位机制
- 记录故障发生时间和次数

### 4. 测试验证规范
- 逐个测试所有输入输出
- 验证所有保护功能
- 测试故障恢复机制
- 确认状态转换逻辑

## 常见问题解决

### Q1: XML导入失败
**解决方案**: 
- 检查XML格式是否符合IEC 61131-10标准
- 确认编码为utf-8
- 验证所有AddData元素完整

### Q2: 变量地址冲突
**解决方案**:
- 使用I/O分配表统一管理地址
- 避免重复使用相同地址
- 预留足够的地址空间

### Q3: 程序运行异常
**解决方案**:
- 检查所有定时器和计数器的复位条件
- 验证状态机的所有分支
- 确认保护逻辑的正确性

## 模板扩展指南

### 添加新的功能模块
1. 在变量定义区添加相关变量
2. 在主程序中添加对应的处理逻辑
3. 更新I/O分配表
4. 添加相应的注释说明

### 集成第三方功能块
1. 在程序开头声明功能块实例
2. 在主程序中调用功能块
3. 配置功能块的输入输出参数
4. 处理功能块的状态和错误信息

---
*版本: V1.0*  
*更新日期: 2026-01-11*  
*适用于: GX Works3 + FX5U PLC*
